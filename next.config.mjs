/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 'rukminim2.flixcart.com', 'images.unsplash.com'],
      },
};

export default nextConfig;
